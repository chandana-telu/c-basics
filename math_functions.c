#include <stdio.h>
#include <math.h>

int main(){

//math operators
 double A = sqrt(2); //square root
 double B = pow(2, 4); // two to the power of four
 int C = round(3.14); //round the no.
 int D = ceil(3.14); //round the no up
 int E = floor(3.99); //round the no down
 double F = fabs(-100); // find the absolute of the no
 double G = log(3); //logarithms
 double H = sin(45); //trigonometric functions
 double I = cos(47);
 double J = tan(67);

 printf("\n%1f",A);
 printf("\n%1f",B);
 printf("\n%d",C);
 printf("\n%d",D);
 printf("\n%d",E);
 printf("\n%d",C);
 printf("\n%1f",F);
 printf("\n%1f",G);
 printf("\n%1f",H);
 printf("\n%1f",I);
 printf("\n%1f",J);

return 0;
}
