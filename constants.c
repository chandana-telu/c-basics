#include <stdio.h>

int main(){

    // constant = a fixed value that cannot be altered by the program during its execution

    const float PI = 3.14159;//to make the item constant
    printf("%f", PI);

    return 0;
}